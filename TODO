See how env works on mac, see if clean environment works i.e env -i bash. On mac bash just fetch the correct environ variables from somewhere

Test cd access permissions on school mac

Test setenv with multiple mixed <VARNAME> <VALUE> or VARNAME=VALUE patterns

TODO: unsetenv tilde

GNL is leaking, probably it reads only to nl and returns
unset path and home and see what happens
